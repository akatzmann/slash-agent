## 1. Merge Conflict Resolution

- [x] 1.1 Resolve `slash_agent/tools.py` conflict markers: update `run_command_windows` signature to `Tuple[int, str, str]` and merge risk patterns.
- [x] 1.2 Resolve `openspec/specs/shell-agent/spec.md` conflict markers by consolidating all requirements.

## 2. Verification

- [x] 2.1 Verify `unittest discover tests` execution.
- [x] 2.2 Verify clean `git status` merge completion.
