## 1. Installer PowerShell Menu Alignment

- [x] 1.1 Add Option `[3] Local OpenAI API` to `bin/installer.ps1` and align menu choices 1–5 with `bin/installer.sh`.
- [x] 1.2 Implement `local_openai` prompt handler in `bin/installer.ps1` asking for endpoint URL, model name, and optional API key.

## 2. Verification

- [x] 2.1 Verify menu choices and `.env` output generation in `bin/installer.ps1`.
